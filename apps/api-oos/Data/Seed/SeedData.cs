namespace ApiOos.Data.Seed;

using ApiOos.Data;

public static class SeedData
{
    public static async Task InitializeAsync(AppDbContext context)
    {
        // Seed initial database records (products, categories) in Epic 3
        await Task.CompletedTask;
    }
}
